{
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "startCommand": "/usr/bin/supervisord -c /etc/supervisord.conf",
        "healthcheckPath": "/health",
        "port": 80
    }
}
